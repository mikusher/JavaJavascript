var Object = {"array":[
	{"class": "one"},
	{"class": "two"},
	{"class": "three"}
]};

for (let index = 0; index < Object.length; index++) {
    const element = Object[index];
    console.log(element);
}