início {
    
    //Escreva um algoritmo que solicita ao usuário 2 valores reais via teclado e depois exibe a soma 
    //dos números fornecidos

    inteiro: num1;
    inteiro: num2;
    inteiro: soma;
    
    leia ("numero 1 ", num1);
    leia ("numero 2 ", num2);
    
    soma = num1 + num2;
    
    escreva ("A soma é: ", soma);
}
